# Build / dependency output (currently none — kept for future tooling)
node_modules/
dist/
.vercel/

# Verification artefacts (local-only screenshots, playwright runs)
.playwright-mcp/
phase*-*.png

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Claude Code agent-isolation worktrees (local scratch, not project state)
.claude/worktrees/
